(ocamllex
    (modules lexer)
)

(menhir
    (modules parser)
    (flags --unused-tokens --explain)
)

(executable
    (name ofluxl)
    (preprocess (pps ppx_jane js_of_ocaml-ppx))
    (libraries base stdio menhirLib js_of_ocaml)
)
